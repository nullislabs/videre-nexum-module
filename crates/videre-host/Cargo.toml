[package]
name = "videre-host"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "The videre venue platform as one nexum-runtime extension: the venue-adapter provider kind, the venue registry service, the advisory egress-guard seam, and the videre:venue/client interface."

[lints]
workspace = true

[dependencies]
# The runtime seam this crate plugs into; the only nexum crate edge.
nexum-runtime = { git = "https://github.com/nullislabs/nexum-runtime", tag = "v0.1.0" }
# Encoder for the opaque status body the host event stream carries; the
# registry lowers an adapter-reported status through it.
nexum-status-body = { git = "https://github.com/nullislabs/nexum-runtime", tag = "v0.1.0" }

wasmtime.workspace = true
anyhow.workspace = true
thiserror.workspace = true
async-trait.workspace = true
futures.workspace = true
serde.workspace = true
tokio.workspace = true
toml.workspace = true
tracing.workspace = true

[dev-dependencies]
nexum-runtime = { git = "https://github.com/nullislabs/nexum-runtime", tag = "v0.1.0", features = ["test-utils"] }
nexum-tasks = { git = "https://github.com/nullislabs/nexum-runtime", tag = "v0.1.0" }
tempfile.workspace = true
