[package]
name = "videre-status-body"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Versioned codec for the opaque status body the host event stream carries: a leading version tag, then that version's borsh payload; unknown tags fail closed."

[lints]
workspace = true

[dependencies]
borsh.workspace = true
thiserror.workspace = true
