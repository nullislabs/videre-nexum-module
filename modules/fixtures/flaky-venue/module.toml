# flaky-venue adapter manifest - the trap-to-recovery test fixture.
# Declares the chain capability its poison-sentinel read requires.

[module]
name = "flaky-venue"
version = "0.1.0"
kind = "venue-adapter"

[capabilities]
required = ["chain"]
optional = []

[capabilities.http]
allow = []

[config]
name = "flaky-venue"
